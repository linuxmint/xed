pythonconsole_sources = [
	'__init__.py',
	'config.py',
	'console.py'
]

install_data(
    pythonconsole_sources,
    install_dir: join_paths(pluginslibdir, 'pythonconsole')
)

install_data(
    'config.ui',
    install_dir: join_paths(pluginsdatadir, 'pythonconsole', 'ui')
)
